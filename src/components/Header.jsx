export default function Header() {
    return (
        <header className="app-header">
            <h1>Tenzi</h1>
            <p>
                Roll untill all dice are the same. Click each die to freeze it
                at its current value between rolls
            </p>
        </header>
    );
}
